import {Container, Navbar} from "react-bootstrap";

export function NavBar () { 
    return (
        <Navbar>
            <Container>
                    Nav
            </Container>
        </Navbar>
    )
}